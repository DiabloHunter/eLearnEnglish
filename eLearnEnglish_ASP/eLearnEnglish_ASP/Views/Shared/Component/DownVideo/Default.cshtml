@model IEnumerable<VideoModel>
<div class="row">
    @foreach (var video in Model)
    {
        <div class="col-md-4">
            <partial name="_videoThumbnail" model="video" />
        </div>
    }

</div>

